"use strict";

class Map {

  constructor(selector) {
    this._selector = selector;
  }

  setPoints(points) {
    this._points = points;
  }

  setType(type) {

  }

  update(currentPoint) {
  	this._currentSpeed = currentSpeed;
  }

  render() {

  }
}